---
description: coding-style
globs:
alwaysApply: true
---

When you write code, comments included, always write in english.

This is a modern python project, and it uses ruff with the following rules:
- B (flake8-bugbear) - https://docs.astral.sh/ruff/rules/#flake8-bugbear-b
- C4 (flake8-comprehensions) - https://docs.astral.sh/ruff/rules/#flake8-comprehensions-c4
- E (pycodestyle errors) - https://docs.astral.sh/ruff/rules/#error-e
- F (pyflakes rules) - https://docs.astral.sh/ruff/rules/#pyflakes-f
- N (pep8-naming) - https://docs.astral.sh/ruff/rules/#pep8-naming-n
- W (pycodestyle warnings) - https://docs.astral.sh/ruff/rules/#warning-w
- I (isort) - https://docs.astral.sh/ruff/rules/#isort-i
- UP (pyupgrade) - https://docs.astral.sh/ruff/rules/#pyupgrade-up
Line length is set to 120 characters.

It uses python 3.10+, so try using default typing when adding types, instead of importing Typings. Avoid using "any", and prefer beeing explicit, like using union ("|") if needed.

Use docstring documentation.

Use pydantic v2. Use @field_validator instead of deprecated @validator decorators.

- **Use modern type union syntax**: Use `X | Y` instead of `(X, Y)` in `isinstance()` calls

- Pour faire les tests, utilise directement le CLI, et ne fait plus de fichier de test à la racine

